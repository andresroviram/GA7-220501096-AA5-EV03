/**
 * Navbar — re-exporta el Topbar principal del layout.
 *
 * Usar este import en cualquier lugar que necesite la barra superior:
 *   import Navbar from '@/components/ui/Navbar';
 *
 * Props:
 *   title           string          — título de la página actual
 *   onLogout        () => void
 *   onToggleSidebar () => void
 *   darkMode        boolean
 *   onToggleTheme   () => void
 */
export { default } from '../Topbar';
