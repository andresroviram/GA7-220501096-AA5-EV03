/**
 * Sidebar — re-exporta el Sidebar principal del layout.
 *
 * Usar este import en cualquier lugar que necesite la barra lateral:
 *   import Sidebar from '@/components/ui/Sidebar';
 *
 * Props:
 *   collapsed       boolean
 *   onToggle        () => void
 *   role            string   — 'administrativo' | 'docente' | 'padre'
 */
export { default } from '../Sidebar';
